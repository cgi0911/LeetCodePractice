class Solution(object):
    def searchBreakingPoint(self, nums):
        """
        :type nums: List[int]
        :return: The breaking point i, defined as a[i] > a[(i+1) % n], where n is len(nums)
        :rtype: int
        """
        n = len(nums)
        
        if (n == 0):    return -1        
        if (n == 1):    return 0
        
        
            
        
        
        
    def search(self, nums, target):
        """
        :type nums: List[int]
        :type target: int
        :rtype: int
        """
        return 0