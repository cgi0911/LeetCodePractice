// LeetCode #4: Median of two sorted arrays

#include <iostream>
#include <vector>
#include <algorithm>
#include <random>

using namespace std;

double findMedian (vector<int> &a, vector<int> &b) {
    int m = a.size ();
    int n = b.size ();
}

int main () {
}
